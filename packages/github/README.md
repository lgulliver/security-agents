# @copilot-review-agents/github

Shared GitHub integration utilities: PR comment rendering, check run creation, issue posting, and the `GitHubIntegrationAgent` abstraction.

> This package is a planned extraction. Logic currently lives in `agents/finops/weekly/github_integration.py`.
