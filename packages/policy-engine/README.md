# @copilot-review-agents/policy-engine

Policy evaluation runtime: loads preset YAML, evaluates findings against blocking thresholds, and produces pass/fail verdicts for CI check runs.

> This package is a planned extraction. Policy definitions currently live in `agents/security/policies/` and `presets/`.
