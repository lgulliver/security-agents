# @copilot-review-agents/core

Shared primitives used across all agents: finding schema types, severity definitions, confidence levels, and the base recommendation model.

> This package is a planned extraction. Logic currently lives within individual agent directories.
